"use client";
import { CalendarPlusIcon, ClockClockwiseIcon } from "@phosphor-icons/react";
import type { SVGProps } from "react";

export const CalendarPlus = (props: SVGProps<SVGSVGElement>) => (
  <CalendarPlusIcon {...props} />
);

export const ClockClockwise = (props: SVGProps<SVGSVGElement>) => (
  <ClockClockwiseIcon {...props} />
);